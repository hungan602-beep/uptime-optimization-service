name: Marketplace Mailer

on:
  push:
  workflow_dispatch:

jobs:
  send_mail:
    runs-on: ubuntu-latest
    steps:
      - name: Send mail
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.transip.email
          server_port: 465
          username: info@bossrentall.com
          password: 'Bossrentall2027'
          subject: Test Email from Bossrentall (Marketplace Action)
          body: This is a test email sent from bossrentall.com via Marketplace Action.
          to: oqayuve892@gmail.com
          from: info@bossrentall.com
          secure: true
